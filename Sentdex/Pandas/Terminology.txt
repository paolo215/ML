"""
	
Series :
			One dimensional array (Acts like numpy array). 
			Can be any datetype.
			Vectorized operations.
			s = Series
			Can still have a label
			Dictionary like

DataFrames :
		two dimensional numpy like array. any data type.
		df = data frame	
		Every column can have a label
		Rows is an index
		Dictionary like

Index :
		What the data is associated with. First column is usually the index

Slicing :
		Selecting data.

Move colums / reorganize

Do mathematical operations on rows

Referencing prior rows hard, but possible. i.e. diff.

Built in moving averages (rolling means in pandas)

standard deviation

Graphing is super simple


		
		


"""
